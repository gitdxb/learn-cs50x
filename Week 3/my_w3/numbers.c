#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int numbers[] = {1,2,3,4,5,6,7,8}

    for (int = 0; i < 8; i++)
    {
        if (numbers[i] = 1)
        {
            printf("found\n");
            return 0;
        }

    }
    printf("No.\n");
    return 1;
}